# Security

Do not report sensitive vulnerabilities in public issues. Keep untrusted score/data parsing bounded and treat external archives, model files and datasets as untrusted input.

This project must not execute OMR providers or AI model artifacts from untrusted paths as part of the deterministic core.
